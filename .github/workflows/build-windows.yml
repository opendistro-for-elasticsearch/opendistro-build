name: Build Windows Exe using tar artifacts

on:
  push:
    branches: 
      - rish1397-patch-1
jobs:
  build-es-artifacts:
    name: Build Windows ES Artifacts
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: Build Windows
      run: |
        set -e
        set -u
        export JAVA_HOME=/openjdk12
        export PATH=$JAVA_HOME:$PATH
        #cd elasticsearch/linux_distributions
        #./opendistro-windows-build.sh
        
  build-kibana-artifacts:
    name: Build Kibana Artifacts
    runs-on: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: Build Kibana
      run: |
        #cd kibana/linux_distributions 
        #./opendistro-windows-kibana-build.sh  
  
  test-odfe-windows:
    needs: [build-es-artifacts, build-kibana-artifacts]
    name: Testing exe
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/checkout@v1
        with:
          repository: rishabh6788/odfe-test
          ref: master
          token: ${{ secrets.GitHub_PAT }}    
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - uses: actions/setup-java@v1
        with:
          java-version: '11.0.x'
          java-package: jdk
          architecture: x64
      - name: Run exe 
        run: |
          pip3 install pytest
          pip3 install requests
          pip install awscli --upgrade
          echo inside base
          dir
          #cd elasticsearch\linux_distributions
          #echo running version info
          #set odfe_version=`..\bin\version-info.py --od`
          #echo %odfe_version%
          #cd ..\..
          #set PACKAGE=Open_Distro_For_Elasticsearch_%odfe_version%.exe
          
          aws s3 cp s3://artifacts.opendistroforelasticsearch.amazon.com/downloads/odfe-windows/ode-windows-zip/opendistroforelasticsearch-1.4.0.zip .\
          unzip .\opendistroforelasticsearch-1.4.0.zip 
          dir
          nohup .\opendistroforelasticsearch-1.4.0\bin\elasticsearch.bat &
          echo Waiting for 30s
          ping -n 30 127.0.0.1 >.\out.txt
          cd ../odfe-test/odfe-test
          dir
          pytest
          
          #aws s3 cp s3://odfe-windows/Open_Distro_For_Elasticsearch_1.3.0.exe .\
          #echo base after downloading exe
          #dir
          # unzip odfe-1.2.1.0-win.zip
          # dir
          #.\odfe-1.2.1.0\bin\elasticsearch.bat
          
          #icacls .\Open_Distro_For_Elasticsearch_1.3.0.exe
          #echo running exe
          #.\Open_Distro_For_Elasticsearch_1.3.0.exe -q -dir .\Open_Distro_For_Elasticsearch -console
          
          
          #Waiting for 20s
          #ping -n 20 127.0.0.1 >.\out.txt
          #dir
          #echo running elasticsearch
          #.\Open_Distro_For_Elasticsearch\bin\elasticsearch.bat
          #Running curl
          #curl -XGET https://localhost:9200 -u admin:admin --insecure
          
        
