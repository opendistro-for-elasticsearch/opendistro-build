name: Build Windows Exe using tar artifacts

on:
  push:
    branches: 
      - rish1397-patch-1
jobs:
  build-es-artifacts:
    name: Build Windows ES Artifacts
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: Build Windows
      run: |
        set -e
        set -u
        export JAVA_HOME=/openjdk12
        export PATH=$JAVA_HOME:$PATH
        #cd elasticsearch/linux_distributions
        #./opendistro-windows-build.sh
        
  build-kibana-artifacts:
    name: Build Kibana Artifacts
    runs-on: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
    - name: Build Kibana
      run: |
        #cd kibana/linux_distributions 
        #./opendistro-windows-kibana-build.sh  
  
  test-odfe-windows:
    #needs: [build-es-artifacts, build-kibana-artifacts]
    name: Testing ES ZIP
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/checkout@v1
        with:
          repository: rishabh6788/odfe-test
          ref: master
          token: ${{ secrets.GitHub_PAT }}  
      - uses: actions/checkout@v1
        with:
          repository: rish1397/KibanaTest
          ref: master
          token: ${{ secrets.GitHub_PAT }}
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - uses: actions/setup-java@v1
        with:
          java-version: '11.0.x'
          java-package: jdk
          architecture: x64
        
      - name: test-es
        env:
          S3_PACKAGE: odfe
          PACKAGE: opendistroforelasticsearch
        run: |
          python -m pip install --upgrade pip
          pip3 install pytest
          pip3 install requests
          echo pip3 -version
          pip3 install awscli
          cd elasticsearch\bin\
          $OD_VERSION='python .\version-info --od'
          #FOR /F "tokens=*" %g IN ('python .\version-info --od') do (SET OD_VERSION=%g)
          cd ..\..
          #$S3_PACKAGE=odfe
          #$PACKAGE=opendistroforelasticsearch
          echo downloading zip from S3 and S3_PACKAGE is $S3_PACKAGE and PACKAGE is $PACKAGE and OD_VERSION is $OD_VERSION 
          dir
          aws s3 cp s3://artifacts.opendistroforelasticsearch.amazon.com/downloads/odfe-windows/ode-windows-zip/$S3_PACKAGE-$OD_VERSION.zip .\
          .\elasticsearch\linux_distributions\TestZIP.bat
      
      - name: test-kibana
        run: |
          cd kibana\bin\
          dir
          $OD_VERSION='python .\version-info --od'
          #FOR /F "tokens=*" %g IN ('python .\version-info --od') do (SET OD_VERSION=%g)
          $S3_PACKAGE=odfe-$OD_VERSION-kibana
          cd ..\..
          echo downloading zip from S3 and S3_PACKAGE is %S3_PACKAGE%
          aws s3 cp s3://artifacts.opendistroforelasticsearch.amazon.com/downloads/odfe-windows/ode-windows-zip/$S3_PACKAGE.zip .\
          dir
          .\kibana\linux_distributions\test-kibana-windows.bat
          
       
